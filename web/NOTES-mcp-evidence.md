Approved evidence-panel design implemented. One get_document call can carry up to three selected evidence pages and question-specific interpretation via an optional input; legacy calls remain supported. All other tools remain data-only.

Checks: TypeScript, 10 MCP HTTP tests and 15 real-Chrome browser checks pass. Real Pearl Street source scans and exact-page links verified locally. Real word geometry on this page does not meet highlight alignment checks, so it correctly shows quotation disclosure without a scan overlay. Deployment and developer-connection verification follow.

Release note: Connected AI answers can now pair a short explanation with the original document scan in one compact evidence panel. Switch between cited pages, check supporting wording, and open the full record on the site.
