// Generated by scripts/build-mcp-reader.mjs. Edit reader/client.ts, style.css, shell.html.
export const readerHtml = "<!doctype html><html lang=\"en\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>9/11 City Records — evidence</title><style>:root{color-scheme:light dark;--ink:#17232b;--muted:#607079;--line:#ccd3d6;--soft:#eef1f2;--paper:#fff;--blue:#214fbb;--navy:#142c3d;--highlight:#e7edc4;--active:#dce5a0;--selected:#eaf0f7;--mono:ui-monospace,SFMono-Regular,Consolas,monospace}\n:root[data-theme=dark]{--ink:#e5ebef;--muted:#a7b7c2;--line:#45545f;--soft:#24323b;--paper:#17232b;--blue:#a5c1ff;--navy:#d2e0e9;--highlight:#444c24;--active:#636d30;--selected:#293c50}\n@media(prefers-color-scheme:dark){:root:not([data-theme=light]){--ink:#e5ebef;--muted:#a7b7c2;--line:#45545f;--soft:#24323b;--paper:#17232b;--blue:#a5c1ff;--navy:#d2e0e9;--highlight:#444c24;--active:#636d30;--selected:#293c50}}\n*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:14px/1.6 Arial,Helvetica,sans-serif}button,select{font:inherit;color:inherit}button{cursor:pointer;background:transparent;border:0}button:hover,a:hover{text-decoration:underline}button:disabled{opacity:.45;cursor:default}button:focus-visible,a:focus-visible,summary:focus-visible{outline:2px solid var(--blue);outline-offset:3px}a{color:var(--blue);text-decoration:none}[hidden]{display:none!important}#reader{border:1px solid var(--line);border-top:3px solid var(--navy);margin-top:0}.brand{display:flex;align-items:center;gap:12px;padding:6px 14px;font-size:11px;min-height:34px}.brand strong{font-size:12px}.brand span{color:var(--muted)}.brand button{margin-left:auto;font-size:11px;color:var(--muted);padding:3px 0}.sources{display:flex;border-top:1px solid var(--line);border-bottom:1px solid var(--line);gap:18px;padding:0 14px}.sources button{padding:9px 0 8px;font-size:11px;border-bottom:2px solid transparent;color:var(--muted);text-align:left}.sources button[aria-pressed=true]{color:var(--blue);border-color:var(--blue)}.sources span{font:10px ui-monospace,monospace;margin-right:5px}.evidence-body{display:grid;grid-template-columns:43% 57%}.meaning{padding:18px 20px 16px}.eyebrow{font-size:9px;letter-spacing:1px;color:var(--muted)}h2{font-size:19px;line-height:1.35;font-weight:400;letter-spacing:-.25px;margin:9px 0 10px}.meaning>p{font-size:13px;line-height:1.65;margin:0 0 16px}.limit{border-left:2px solid var(--line);padding-left:10px;font-size:12px}.limit strong{font-size:11px;font-weight:600}.limit p{margin:4px 0 0;line-height:1.65}details{margin:14px 0;font-size:11px}summary{cursor:pointer;color:var(--blue)}blockquote{margin:10px 0;padding:7px 9px;background:#f7f8f4;font-size:12px}.small{font-size:10px!important;line-height:1.5!important;color:var(--muted)}.primary{display:inline-block;background:var(--navy);color:#fff;padding:8px 12px;border-radius:2px;font-size:12px;margin-top:2px}.meaning .attribution{margin:9px 0 0;font-size:9px!important}.scan{padding:10px 12px 8px;background:var(--soft);border-left:1px solid var(--line);min-width:0}.scan-bar,.scan-caption{display:flex;align-items:center;justify-content:space-between;gap:10px;font-size:10px;color:var(--muted)}.scan-bar{margin-bottom:8px}.scan-bar button,.scan-caption button{font-size:10px;color:var(--blue);padding:0}.scan-button{position:relative;padding:0;width:100%;display:block;background:white;border:1px solid #d4d9dc;line-height:0}.scan-page img{width:100%;display:block}.scan-highlight{position:absolute;background:rgba(223,232,173,.38);outline:1px solid rgba(120,138,61,.55);pointer-events:none}.scan-caption{margin-top:8px}.scan-caption>.mono{font:9px ui-monospace,SFMono-Regular,Consolas,monospace}footer{display:flex;align-items:center;justify-content:space-between;gap:8px;border-top:1px solid var(--line);padding:7px 14px;font-size:10px;color:var(--muted)}footer button{font-size:10px;color:var(--blue)}.scan-page{position:relative;line-height:0;background:#fff}.scan-button{border:0}.scan-page img{width:100%;display:block}.scan-missing{line-height:1.6;background:var(--soft);padding:30px 18px;min-height:240px;font-size:12px}.scan-missing p{margin-bottom:0}.highlight-note{font-size:10px;line-height:1.4;color:var(--muted);margin:6px 0 0}.highlight-note:empty{display:none}.explanation{overflow-wrap:anywhere;max-height:240px;overflow:auto}.meaning h2{overflow-wrap:anywhere}.sources{overflow-x:auto}.sources button{min-width:0;flex:1;overflow-wrap:anywhere}.status{padding:24px 18px;font-size:13px;color:var(--muted)}.status h2{font-size:16px;color:var(--ink)}.status button{color:var(--blue)}#notice:not(:empty){border-top:1px solid var(--line);padding:8px 14px;font-size:12px}.copy-fallback{display:block;width:100%;min-height:80px}.scan-dialog{width:calc(100% - 20px);max-height:85vh;border:1px solid var(--line);padding:12px;background:var(--paper);color:var(--ink)}.scan-dialog::backdrop{background:#142c3da6}.scan-dialog header{display:flex;align-items:center;justify-content:space-between;font-size:12px;margin-bottom:10px}.scan-dialog img{width:100%;display:block}.scan-dialog>a{display:inline-block;margin-top:12px}.primary{color:var(--paper)}blockquote{background:var(--soft)}@media(max-width:520px){.evidence-body{grid-template-columns:1fr}.scan{border-left:0;border-top:1px solid var(--line)}.brand span{display:none}.meaning{padding:16px}.sources{gap:12px}.sources button{font-size:11px}}@media(pointer:coarse){button,summary,.primary{min-height:44px}.scan-button{min-height:0}}\n</style></head><body><main id=\"reader\" aria-label=\"9/11 City Records evidence\"><header class=\"brand\"><strong>9/11 City Records</strong><span>Evidence for your answer</span><button id=\"collapse\" type=\"button\" aria-expanded=\"true\" aria-controls=\"content\">Hide preview −</button></header><div id=\"content\"><p class=\"status\" role=\"status\">Opening evidence…</p></div><div id=\"notice\" role=\"status\" aria-live=\"polite\"></div></main><script>\"use strict\";\n/** Self-contained evidence panel. Rebuild with node scripts/build-mcp-reader.mjs. */\n(() => {\n    const win = window;\n    const content = document.getElementById('content'), notice = document.getElementById('notice');\n    const collapse = document.getElementById('collapse');\n    const pending = new Map();\n    let rpcId = 0, epoch = 0, bridgeReady = false, parentOrigin = '*', disposed = false, resizeFrame = 0, lastHeight = 0;\n    let data = null, meta = {}, evidence = [], selected = 0, highlights = true;\n    let lastRequest = null;\n    let image = null, scanPage = null, scanCaption = null;\n    let dialog = null;\n    const normalize = (s) => s.normalize('NFKC').replace(/\\s+/gu, ' ').trim();\n    function el(tag, cls = '', text) { const n = document.createElement(tag); n.className = cls; if (text !== undefined)\n        n.textContent = text; return n; }\n    function button(label, fn, cls = '') { const b = el('button', cls, label); b.type = 'button'; b.onclick = fn; return b; }\n    function safeUrl(value) { if (typeof value !== 'string')\n        return null; try {\n        const u = new URL(value);\n        return u.origin === 'https://911records.nyc' && !u.username && !u.password ? u.href : null;\n    }\n    catch {\n        return null;\n    } }\n    function link(label, value, cls = '') { const href = safeUrl(value); if (!href)\n        return null; const a = el('a', cls, label); a.href = href; a.target = '_blank'; a.rel = 'noopener noreferrer'; return a; }\n    function notify(method, params) { if (window.parent !== window)\n        window.parent.postMessage({ jsonrpc: '2.0', method, params }, parentOrigin); }\n    function request(method, params, timeout = 30000) {\n        return new Promise((resolve, reject) => {\n            if (window.parent === window || disposed) {\n                reject(new Error('Open this reader in a connected MCP app.'));\n                return;\n            }\n            const id = ++rpcId;\n            const timer = setTimeout(() => { pending.delete(id); reject(new Error('The host did not respond. Please retry.')); }, timeout);\n            pending.set(id, { resolve, reject, timer });\n            window.parent.postMessage({ jsonrpc: '2.0', id, method, params }, parentOrigin);\n        });\n    }\n    document.addEventListener('click', e => {\n        const a = e.target.closest('a');\n        if (!a)\n            return;\n        const href = safeUrl(a.href);\n        if (!href) {\n            e.preventDefault();\n            return;\n        }\n        if (bridgeReady) {\n            e.preventDefault();\n            void request('ui/open-link', { url: href }).catch(() => { notice.textContent = 'The host could not open this link. Use the link menu to open it in your browser.'; });\n        }\n        else if (win.openai?.openExternal) {\n            e.preventDefault();\n            win.openai.openExternal({ href, redirectUrl: false });\n        }\n    });\n    function setContext(c) { if (c.theme === 'light' || c.theme === 'dark')\n        document.documentElement.dataset.theme = c.theme; }\n    collapse.onclick = () => { content.hidden = !content.hidden; collapse.setAttribute('aria-expanded', String(!content.hidden)); collapse.textContent = content.hidden ? 'Show evidence +' : 'Hide preview −'; };\n    function clearView() { dialog?.close(); dialog?.remove(); dialog = null; image = null; scanPage = scanCaption = null; content.replaceChildren(); notice.replaceChildren(); }\n    function status(title, detail, retry = false) { clearView(); data = null; meta = {}; const box = el('section', 'status'); box.setAttribute('role', 'status'); box.append(el('h2', '', title), el('p', '', detail)); if (retry && lastRequest)\n        box.append(button('Retry', () => { if (lastRequest)\n            void call(lastRequest.name, lastRequest.args); })); content.append(box); content.setAttribute('aria-busy', 'false'); }\n    function accept(result, fresh = false) {\n        if (fresh) {\n            evidence = [];\n            selected = 0;\n            highlights = true;\n            lastRequest = null;\n        }\n        content.setAttribute('aria-busy', 'false');\n        if (result.isError || !result.structuredContent) {\n            evidence = [];\n            const message = result.content?.filter(c => c.type === 'text').map(c => c.text || '').join(' ') || 'No record was returned.';\n            status('Record unavailable', message, !/removed|does not exist/i.test(message));\n            return;\n        }\n        data = result.structuredContent;\n        meta = result._meta?.reader || {};\n        if (meta.evidence?.length) {\n            evidence = meta.evidence;\n            selected = 0;\n            const source = evidence[0]?.source;\n            if (source?.structuredContent && !source.isError) {\n                data = source.structuredContent;\n                meta = source._meta?.reader || {};\n            }\n        }\n        render();\n        // Older connections may return a document without the enriched evidence payload.\n        if (data?.pages?.length && (bridgeReady || win.openai?.callTool))\n            void call('get_page', { doc: data.doc, page: data.pages[0].page });\n    }\n    async function call(name, args) {\n        const generation = ++epoch;\n        lastRequest = { name, args };\n        status('Opening evidence…', 'Loading the selected source page.');\n        content.setAttribute('aria-busy', 'true');\n        try {\n            const result = bridgeReady ? await request('tools/call', { name, arguments: args }) : win.openai?.callTool ? await win.openai.callTool(name, args) : (() => { throw new Error('Open this reader in a connected MCP app.'); })();\n            if (generation !== epoch || disposed)\n                return;\n            accept(result);\n        }\n        catch (e) {\n            if (generation !== epoch || disposed)\n                return;\n            evidence = [];\n            status('Unable to open evidence', e instanceof Error ? e.message : 'Please retry.', true);\n        }\n    }\n    function currentBrief() { const b = evidence[selected]; return b && b.doc === data?.doc && b.page === data?.page ? b : undefined; }\n    function verifiedQuote() { const q = currentBrief()?.quote; return q && data?.text && normalize(data.text).includes(normalize(q)) ? q : null; }\n    function render() {\n        clearView();\n        if (!data)\n            return;\n        if (typeof data.text !== 'string') {\n            const box = el('section', 'status');\n            box.append(el('h2', '', data.machine_extracted_title || 'Records'));\n            if (data.pages?.length)\n                box.append(el('p', '', 'Opening the cited page…'));\n            else {\n                box.append(el('p', '', 'Open a record on the site to explore the source.'));\n                const rows = data.hits || data.documents || data.changes || [];\n                for (const row of rows) {\n                    if ('kind' in row && row.kind === 'removed')\n                        continue;\n                    const a = link(row.doc, row.url);\n                    if (a)\n                        box.append(a, el('br'));\n                }\n            }\n            const a = link('Open full record ↗', data.url, 'primary');\n            if (a)\n                box.append(a);\n            content.append(box);\n            return;\n        }\n        const page = data, brief = currentBrief();\n        // When a source changes, never retain a claim tied to a quotation that no longer matches.\n        const interpretation = brief?.claim && (!brief.quote || verifiedQuote()) ? brief : undefined;\n        if (evidence.length > 1) {\n            const nav = el('nav', 'sources');\n            nav.setAttribute('aria-label', 'Cited pages');\n            evidence.forEach((s, i) => { const b = button((i + 1) + '  ' + (s.label || 'Page ' + s.page), () => { selected = i; void call('get_page', { doc: s.doc, page: s.page, max_chars: 20000 }); }); b.setAttribute('aria-pressed', String(i === selected)); nav.append(b); });\n            content.append(nav);\n        }\n        const body = el('div', 'evidence-body'), meaning = el('section', 'meaning');\n        meaning.setAttribute('aria-label', 'Source interpretation');\n        meaning.append(el('div', 'eyebrow', interpretation ? 'WHY THIS PAGE MATTERS' : 'DOCUMENT SUMMARY'));\n        meaning.append(el('h2', '', interpretation?.claim || meta.title || 'Source page ' + page.page));\n        meaning.append(el('p', 'explanation', interpretation?.explanation || meta.summary || 'No summary is available for this document yet. Open the full record to explore the source.'));\n        if (interpretation?.limitation) {\n            const limit = el('div', 'limit');\n            limit.append(el('strong', '', 'What this doesn’t establish'), el('p', '', interpretation.limitation));\n            meaning.append(limit);\n        }\n        const quote = verifiedQuote();\n        if (quote) {\n            const details = el('details');\n            details.append(el('summary', '', 'Check the wording'), el('blockquote', '', quote), el('p', 'small', 'Machine-extracted wording · Check the scan for context.'));\n            meaning.append(details);\n        }\n        const open = link('Open full record ↗', page.url, 'primary');\n        if (open)\n            meaning.append(open);\n        meaning.append(el('p', 'small attribution', interpretation ? 'AI interpretation · Verify against the source' : 'Machine-generated document summary · May describe other pages'));\n        const scan = el('section', 'scan');\n        scan.setAttribute('aria-label', 'Original document scan');\n        const bar = el('div', 'scan-bar');\n        const pageLabel = 'Original scan · page ' + page.page + (meta.pageCount ? ' of ' + meta.pageCount : '');\n        bar.append(el('span', '', pageLabel));\n        const enlarge = button('Enlarge ↗', () => showScan(pageLabel));\n        bar.append(enlarge);\n        scan.append(bar);\n        scanPage = el('div', 'scan-page');\n        const url = safeUrl(page.scan_url);\n        const missing = () => { enlarge.disabled = true; image = null; scanPage?.replaceChildren(el('strong', '', 'Scan unavailable'), el('p', '', 'Open the full record to check the original page.')); scanPage?.classList.add('scan-missing'); if (scanCaption)\n            scanCaption.textContent = ''; };\n        if (url) {\n            const img = el('img');\n            image = img;\n            img.alt = 'Original scan, ' + page.bates + ', page ' + page.page;\n            img.referrerPolicy = 'no-referrer';\n            img.onload = () => { if (image === img)\n                highlightScan(); };\n            img.onerror = () => { if (image === img)\n                missing(); };\n            const b = button('', () => showScan(pageLabel), 'scan-button');\n            b.setAttribute('aria-label', 'Enlarge original document scan');\n            b.append(img);\n            scanPage.append(b);\n            img.src = url;\n        }\n        else\n            missing();\n        scan.append(scanPage);\n        const stamp = el('div', 'scan-caption');\n        stamp.append(el('span', 'mono', page.bates || page.doc || ''));\n        if (quote) {\n            const toggle = button(highlights ? 'Highlight on' : 'Highlight off', () => { highlights = !highlights; toggle.textContent = highlights ? 'Highlight on' : 'Highlight off'; toggle.setAttribute('aria-pressed', String(highlights)); highlightScan(); });\n            toggle.setAttribute('aria-pressed', String(highlights));\n            stamp.append(toggle);\n        }\n        scan.append(stamp);\n        scanCaption = el('p', 'highlight-note');\n        scan.append(scanCaption);\n        body.append(meaning, scan);\n        content.append(body);\n        const foot = el('footer');\n        foot.append(el('span', '', meta.agency || 'Independent public records mirror'), button('Copy citation', () => { void copyCitation(page); }));\n        content.append(foot);\n    }\n    function highlightScan() {\n        scanPage?.querySelectorAll('.scan-highlight').forEach(n => n.remove());\n        if (scanCaption)\n            scanCaption.textContent = '';\n        const quote = verifiedQuote();\n        if (!quote || !highlights || !image?.naturalWidth || !scanPage)\n            return;\n        const boxes = meta.boxes;\n        let aligned = false;\n        if (boxes && boxes.w > 0 && boxes.h > 0 && !(data?.offset) && data?.next_offset == null && Math.abs(image.naturalWidth / image.naturalHeight / (boxes.w / boxes.h) - 1) < .03) {\n            const text = boxes.words.map(w => w[4]).join(' '), normalized = normalize(text), phrase = normalize(quote);\n            if (normalized === normalize(data?.text || '')) {\n                // Build offsets in the same normalized string used for matching; skip\n                // uncertain mappings rather than drawing a plausible but incorrect box.\n                const tokens = boxes.words.map(w => normalize(w[4]));\n                if (tokens.join(' ') === normalized) {\n                    let start = 0, count = 0;\n                    while ((start = normalized.indexOf(phrase, start)) >= 0 && count++ < 20) {\n                        let cursor = 0;\n                        for (let i = 0; i < tokens.length; i++) {\n                            const end = cursor + tokens[i].length;\n                            if (end > start && cursor < start + phrase.length) {\n                                const w = boxes.words[i];\n                                const mark = el('span', 'scan-highlight');\n                                mark.style.left = 100 * w[0] / boxes.w + '%';\n                                mark.style.top = 100 * w[1] / boxes.h + '%';\n                                mark.style.width = 100 * (w[2] - w[0]) / boxes.w + '%';\n                                mark.style.height = 100 * (w[3] - w[1]) / boxes.h + '%';\n                                scanPage.append(mark);\n                                aligned = true;\n                            }\n                            cursor = end + 1;\n                        }\n                        start += phrase.length;\n                    }\n                }\n            }\n        }\n        if (scanCaption)\n            scanCaption.textContent = aligned ? 'Quoted wording highlighted · Check the scan' : 'Exact scan highlight unavailable · Use “Check the wording”';\n    }\n    function showScan(label) {\n        if (!image || !data)\n            return;\n        dialog?.remove();\n        dialog = el('dialog', 'scan-dialog');\n        const head = el('header');\n        head.append(el('strong', '', label), button('Close ×', () => dialog?.close()));\n        const img = el('img');\n        img.src = image.src;\n        img.alt = image.alt;\n        dialog.append(head, img);\n        const a = link('Open full record ↗', data.url);\n        if (a)\n            dialog.append(a);\n        document.getElementById('reader').append(dialog);\n        dialog.showModal();\n    }\n    async function copyCitation(page) {\n        const url = safeUrl(page.short_url) || safeUrl(page.url);\n        if (!url)\n            return;\n        const text = (page.bates || page.doc) + ' · page ' + page.page + '\\n' + url;\n        try {\n            await navigator.clipboard.writeText(text);\n            notice.textContent = 'Citation copied.';\n        }\n        catch {\n            const area = el('textarea', 'copy-fallback');\n            area.readOnly = true;\n            area.value = text;\n            area.setAttribute('aria-label', 'Citation to copy');\n            notice.replaceChildren(el('span', '', 'Copy this citation:'), area);\n            area.focus();\n            area.select();\n        }\n    }\n    window.addEventListener('message', event => {\n        if (event.source !== window.parent)\n            return;\n        const msg = event.data;\n        if (!msg || msg.jsonrpc !== '2.0')\n            return;\n        if (parentOrigin !== '*' && event.origin !== parentOrigin)\n            return;\n        if (msg.id !== undefined && pending.has(msg.id)) {\n            const p = pending.get(msg.id);\n            pending.delete(msg.id);\n            clearTimeout(p.timer);\n            if (event.origin && event.origin !== 'null')\n                parentOrigin = event.origin;\n            if (msg.error)\n                p.reject(new Error('The host could not complete this request.'));\n            else\n                p.resolve(msg.result);\n            return;\n        }\n        if (msg.method === 'ui/notifications/tool-input') {\n            ++epoch;\n            evidence = [];\n            lastRequest = null;\n            status('Opening evidence…', 'Loading source pages.');\n            content.setAttribute('aria-busy', 'true');\n        }\n        if (msg.method === 'ui/notifications/tool-result') {\n            ++epoch;\n            accept(msg.params || {}, true);\n        }\n        if (msg.method === 'ui/notifications/tool-cancelled') {\n            ++epoch;\n            evidence = [];\n            lastRequest = null;\n            status('Request cancelled', 'Ask to open the record again when you are ready.');\n        }\n        if (msg.method === 'ui/notifications/host-context-changed')\n            setContext(msg.params || {});\n        if (msg.method === 'ui/resource-teardown') {\n            disposed = true;\n            ++epoch;\n            pending.forEach(p => { clearTimeout(p.timer); p.reject(new Error('Reader closed.')); });\n            pending.clear();\n            observer.disconnect();\n            cancelAnimationFrame(resizeFrame);\n            evidence = [];\n            clearView();\n            window.parent.postMessage({ jsonrpc: '2.0', id: msg.id, result: {} }, parentOrigin);\n        }\n    });\n    function legacyResult(globals) { setContext(globals); if (!bridgeReady && 'toolOutput' in globals) {\n        ++epoch;\n        accept({ structuredContent: globals.toolOutput, _meta: globals.toolResponseMetadata || win.openai?.toolResponseMetadata }, true);\n    } }\n    window.addEventListener('openai:set_globals', e => legacyResult(e.detail?.globals || {}));\n    const observer = new ResizeObserver(() => { cancelAnimationFrame(resizeFrame); resizeFrame = requestAnimationFrame(() => { if (disposed)\n        return; const height = Math.ceil(document.getElementById('reader').getBoundingClientRect().height); if (height === lastHeight)\n        return; lastHeight = height; if (bridgeReady)\n        notify('ui/notifications/size-changed', { height });\n    else\n        win.openai?.notifyIntrinsicHeight?.(height); }); });\n    observer.observe(document.getElementById('reader'));\n    if (win.openai)\n        legacyResult(win.openai);\n    void request('ui/initialize', { protocolVersion: '2026-01-26', appInfo: { name: '911records-reader', version: '1.2.0' }, appCapabilities: {} }, 5000)\n        .then(value => { if (disposed)\n        return; bridgeReady = true; setContext(value.hostContext || {}); notify('ui/notifications/initialized', {}); if (data?.pages?.length)\n        void call('get_page', { doc: data.doc, page: data.pages[0].page }); })\n        .catch(() => { if (!win.openai?.callTool && !data && !disposed)\n        status('Connect to read records', 'Open this reader through the 9/11 City Records app.'); });\n})();\n</script></body></html>\n";
